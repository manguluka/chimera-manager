extends ../layout.pug

block title
  | Cancel #{event.title}

block content

  p.text-center.mb-5
    a(href=`/events/${event.id}`)
      i.ti-arrow-left.mr-2
      | Back to event

  .row
    .col-sm-6.offset-sm-3
      h1.page-heading Cancel "#{event.title}"?
      p This will:
      ul
        li Remove the event from the website
        li Notify all attendees and instructors of the cancellation
        li Refund attendees
        if event.category === 'meetup'
          li Cancel the event on Meetup.com
      p
        strong Note:
        br
        | This will <u>not</u> permanently delete this event. You can always re-publish it at a later point if you want.
      p.mt-5
        form(action=`/events/${event.id}/cancel` method='post')
          button.btn.btn-danger.btn-lg.btn-block(type='submit')
            i.ti-close.mr-2
            | Cancel Event?
